[workspace]
resolver = "2"
members = ["."]

[package]
name = "sgl-kv-indexer"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Redis-backed KV cache indexer with a SGLang KV-event bridge"
publish = false  # experimental crate; not published to crates.io

[lib]
name = "sgl_kv_indexer"
path = "src/lib.rs"

[features]
default = ["bridge"]
# The SGLang event follower. On by default so the standard build still produces
# both binaries; a routing client turns it off to depend on the gRPC client and
# generated types without ZMQ and msgpack.
bridge = ["dep:rmpv", "dep:zeromq"]
# Enables the Redis storage backend. Kept off by default so the standard build
# does not pull the redis client and its transitive deps.
redis-backend = ["dep:redis", "dep:futures"]

[[bin]]
name = "kv-indexer-server"
path = "src/bin/kv-indexer-server.rs"

[[bin]]
name = "kv-indexer-bridge"
path = "src/bin/kv-indexer-bridge.rs"
required-features = ["bridge"]

[dependencies]
bytes = "1"
futures = { version = "0.3", optional = true }
prost = "0.14"
redis = { version = "0.27", optional = true, features = [
    "tokio-comp",
    "cluster-async",
    "connection-manager",
] }
rmpv = { version = "1.3.1", optional = true }
tokio = { version = "1", features = ["macros", "rt-multi-thread", "signal", "time"] }
tonic = { version = "0.14.6", features = ["transport"] }
tonic-health = "0.14"
tonic-prost = "0.14.6"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt"] }
zeromq = { version = "0.6.0", optional = true }

[build-dependencies]
tonic-prost-build = "0.14.6"
